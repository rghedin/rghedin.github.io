---
layout: default
---

    <ul class="listagem-posts">
    {% for post in site.posts %}
      <li><a class="post-link" href="{{ post.url | relative_url }}">{{ post.title | escape }}</a> <aside>{{ post.date | date: "%b %-d, %Y" }}</aside></li>
    {% endfor %}
    </ul>
